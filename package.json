{
  "name": "thread",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "levels": "tsx scripts/build-levels.ts",
    "validate": "tsx scripts/validate.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "e2e": "playwright test",
    "typecheck": "tsc --noEmit",
    "ci": "pnpm typecheck && pnpm test && pnpm validate && pnpm build",
    "verify:deploy": "node scripts/verify-deploy.mjs",
    "compare:reference": "node scripts/compare-reference.mjs",
    "verify:bytes": "node scripts/verify-deployed-bytes.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^26.2.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.7",
    "vitest": "^2.1.8"
  }
}
